# Loaded by "npm test". A dedicated database and Redis, so the suite can never touch
# development data. Nothing secret here: these are the throwaway local credentials.
# (In CI the environment overrides these values.)
DATABASE_URL=postgres://monitor:monitor@localhost:5432/api_monitor_test
REDIS_HOST=localhost
REDIS_PORT=6390
